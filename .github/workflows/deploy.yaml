
name: deploy to server

on: 
  push:
    branches:
      - main

jobs: 
  first_job:
    runs-on: ubuntu-latest
    env:
      name: "CodigoFacilito"
    steps: 
      - name: Hola mundo
        run: echo "Hola mundo desde GitHub Actions $CodigoFacilito"
  second_job:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: first_job
    runs-on: ubuntu-latest

    steps: 
      - name: Hola mundo
        run: echo "Hola mundo desde GitHub Actions 2"
    